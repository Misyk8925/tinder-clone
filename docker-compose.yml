services:

  redis:
    image: redis:8.2.1-alpine
    restart: "no"
    ports:
      - "6379:6379"
    healthcheck:
      test: redis-cli ping
      interval: 10s
      timeout: 5s
      start_period: 10s
      retries: 5


networks:
  supabase_network_michael:
    external: true