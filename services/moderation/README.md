# Moderation

Own service for classifier commands, policy versions, and review tasks. It **is** in the Compose stack and in the root README maps.

It is **not** a six-minute demo story. Phase 5 (live providers / release) is unfinished. If an interviewer asks, say that inline checks in Profiles/Match were rejected because this is a different consistency domain — then go back to Discover / outbox / gateway.

Feature index: [`docs/features/complete-moderation-service`](docs/features/complete-moderation-service/README.md).
