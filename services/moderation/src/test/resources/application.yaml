spring:
  datasource:
    url: jdbc:h2:mem:moderation;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  flyway:
    enabled: false

moderation:
  persistence:
    mode: memory
  security:
    users: []
