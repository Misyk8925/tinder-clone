package com.tinder.profiles.domain.profile;

/** Business significance of a profile change. */
public enum ProfileChangeType {
    PREFERENCES,
    CRITICAL_FIELDS,
    LOCATION_CHANGE,
    NON_CRITICAL
}
