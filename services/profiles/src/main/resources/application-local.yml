spring:
  config:
    activate:
      on-profile: local
  kafka:
    bootstrap-servers: ${PROFILES_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
  datasource:
    url: ${PROFILES_DB_URL:jdbc:postgresql://127.0.0.1:5435/profiles_db}
    username: ${PROFILES_DB_USER:profiles_app}
    password: ${PROFILES_DB_PASSWORD:change_me_profiles_db_password}
  ssl:
    bundle:
      jks:
        grpc-server-tls:
          keystore:
            location: classpath:profiles-service.p12
            password: changeit
            type: PKCS12
          truststore:
            location: classpath:truststore.jks
            password: changeit
            type: JKS

internal:
  server:
    ssl:
      key-store: classpath:profiles-service.p12
      key-store-password: changeit
      trust-store: classpath:truststore.jks
      trust-store-password: changeit
