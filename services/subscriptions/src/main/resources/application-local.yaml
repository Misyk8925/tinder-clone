spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: ${SUBSCRIPTIONS_DB_URL:jdbc:postgresql://127.0.0.1:5435/subscriptions_db}
    username: ${SUBSCRIPTIONS_DB_USER:subscriptions_app}
    password: ${SUBSCRIPTIONS_DB_PASSWORD:change_me_subscriptions_db_password}
